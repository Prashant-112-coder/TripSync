export default function Placeholder({ title, description }) {
  return <section className="placeholder-page"><div className="placeholder-icon">✦</div><span>TRIPSYNC</span><h1>{title}</h1><p>{description}</p><div className="placeholder-note">This module is part of the phased build. The UI foundation is ready.</div></section>;
}
